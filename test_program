local procedure test()
begin
    begin
    end;
end;

trigger OnRun()
var
    a: Decimal;
    x: Boolean;
    b: Text;
begin
    b := '123' + '123' + 'w123';
    x := false;
    a := 1 / (a - 1) * 33 - test() - 1 - 3;
    test();
end;
